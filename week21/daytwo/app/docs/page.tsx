export default function Docs () {
    return <div>
        <h1>we use [...name] the folder name and we can catch all the routes after localhost:3000/api/blogs/ANYTHING/ANYTHING... as we write the page.tsx inside a folder name :  [...name]</h1>
    </div>
}